---
phase: quick-260324-i2p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/api/feature-toggles/index.post.ts
  - server/utils/validators.ts
  - app/pages/dashboard/feature-toggles.vue
autonomous: true
requirements: [CREATE-TOGGLE]

must_haves:
  truths:
    - "Superadmin can open a create toggle form from the feature toggles page"
    - "Superadmin can submit key, label, description, and enabled state to create a new toggle"
    - "New toggle appears in the list after creation without manual refresh"
    - "Non-superadmin requests to create toggles are rejected with 403"
    - "Duplicate key submissions are rejected with a clear error"
  artifacts:
    - path: "server/api/feature-toggles/index.post.ts"
      provides: "POST endpoint for creating feature toggles"
      exports: ["default"]
    - path: "server/utils/validators.ts"
      provides: "Zod schema for feature toggle creation"
      contains: "featureToggleCreateSchema"
    - path: "app/pages/dashboard/feature-toggles.vue"
      provides: "Create toggle button and modal form"
      contains: "UModal"
  key_links:
    - from: "app/pages/dashboard/feature-toggles.vue"
      to: "/api/feature-toggles"
      via: "$fetch POST"
      pattern: "\\$fetch.*api/feature-toggles.*POST"
    - from: "server/api/feature-toggles/index.post.ts"
      to: "requireSuperAdmin"
      via: "auth guard"
      pattern: "requireSuperAdmin"
---

<objective>
Add a "Create Toggle" button and modal form on the superadmin feature toggles dashboard page, plus a POST API endpoint to create new feature toggle rows in the database.

Purpose: Superadmins currently cannot create new feature toggles from the UI -- they must be seeded via SQL migrations. This adds self-service toggle creation.
Output: POST endpoint + modal form integrated into existing feature-toggles.vue page.
</objective>

<execution_context>
@/Users/ekasetyanugraha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekasetyanugraha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@server/api/feature-toggles/index.get.ts
@server/api/feature-toggles/[key].patch.ts
@server/utils/validators.ts
@server/utils/auth.ts
@app/pages/dashboard/feature-toggles.vue
@app/components/BranchFormModal.vue

<interfaces>
<!-- Existing patterns the executor needs -->

From server/utils/auth.ts:
```typescript
export async function requireSuperAdmin(event: H3Event): Promise<User>
```

From server/utils/supabase.ts (used by existing toggle endpoints):
```typescript
export function getServiceClient(event: H3Event): SupabaseClient
```

From server/utils/validators.ts (existing toggle schema pattern):
```typescript
export const featureToggleUpdateSchema = z.object({
  enabled: z.boolean(),
})
```

From app/components/BranchFormModal.vue (modal pattern):
```vue
<!-- Uses UModal with v-model:open, #body slot for form, #footer slot for buttons -->
<UModal v-model:open="open" :title="title">
  <template #body>...</template>
  <template #footer>...</template>
</UModal>
```

Database schema (feature_toggles table columns):
```sql
id          UUID DEFAULT gen_random_uuid() PRIMARY KEY,
key         TEXT UNIQUE NOT NULL,
enabled     BOOLEAN NOT NULL DEFAULT false,
label       TEXT NOT NULL,
description TEXT,
updated_at  TIMESTAMPTZ DEFAULT now(),
created_at  TIMESTAMPTZ DEFAULT now()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST API endpoint and validation schema for creating feature toggles</name>
  <files>server/utils/validators.ts, server/api/feature-toggles/index.post.ts</files>
  <action>
1. In `server/utils/validators.ts`, add a `featureToggleCreateSchema` right after the existing `featureToggleUpdateSchema`:
   ```typescript
   export const featureToggleCreateSchema = z.object({
     key: z.string().min(1).max(100).regex(/^[a-z0-9_]+$/, 'Key must be lowercase alphanumeric with underscores only'),
     label: z.string().min(1).max(100),
     description: z.string().max(500).optional(),
     enabled: z.boolean().default(false),
   })
   ```

2. Create `server/api/feature-toggles/index.post.ts`:
   - Call `await requireSuperAdmin(event)` as the first line (same pattern as [key].patch.ts)
   - Read and validate body with `featureToggleCreateSchema.safeParse(body)`, return 400 on failure with `parsed.error.flatten()`
   - Use `getServiceClient(event)` to insert into `feature_toggles` table: `{ key, label, description: description ?? null, enabled }`
   - Handle unique constraint violation on `key` (Supabase/Postgres error code `23505`) -- return 409 with message "Toggle with this key already exists"
   - On generic DB error, return 500
   - On success, return `{ success: true, data: insertedRow }` with the inserted row (select after insert via `.select().single()`)
  </action>
  <verify>
    <automated>cd /Users/ekasetyanugraha/personal-projects/fide-cards && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>POST /api/feature-toggles accepts {key, label, description?, enabled?}, validates with zod, guards with requireSuperAdmin, inserts into DB, returns created row. Duplicate key returns 409.</done>
</task>

<task type="auto">
  <name>Task 2: Add create toggle button and modal form to the feature toggles page</name>
  <files>app/pages/dashboard/feature-toggles.vue</files>
  <action>
Add an inline modal form to the existing feature-toggles.vue page following the BranchFormModal.vue pattern (UModal with v-model:open, #body, #footer slots). Specifically:

1. Add reactive state for the modal:
   ```typescript
   const showCreateModal = ref(false)
   const createForm = reactive({ key: '', label: '', description: '', enabled: false })
   const creating = ref(false)
   ```

2. Add auto-generate key from label (same pattern as BranchFormModal slug generation):
   ```typescript
   watch(() => createForm.label, (label) => {
     createForm.key = label.toLowerCase().replace(/[^a-z0-9]+/g, '_').replace(/(^_|_$)/g, '')
   })
   ```

3. Add a `handleCreate` async function:
   - Set `creating.value = true`
   - POST to `/api/feature-toggles` with `{ key: createForm.key, label: createForm.label, description: createForm.description || undefined, enabled: createForm.enabled }`
   - On success: close modal, reset form fields to defaults, call `refresh()`, show success toast "Toggle berhasil dibuat"
   - On error: show error toast with `e.data?.message || 'Gagal membuat toggle'`
   - Finally: set `creating.value = false`

4. Add a reset watcher on modal open/close:
   ```typescript
   watch(showCreateModal, (isOpen) => {
     if (!isOpen) {
       createForm.key = ''
       createForm.label = ''
       createForm.description = ''
       createForm.enabled = false
     }
   })
   ```

5. In the template, add a "Tambah Toggle" button in the header area (after the subtitle `<p>` tag), inside the existing `mb-3` div. Use:
   ```html
   <div class="flex items-center justify-between mb-3">
     <div>
       <h1 class="text-lg font-semibold">Feature Toggles</h1>
       <p class="text-sm text-(--ui-text-muted)">Kelola fitur yang aktif di aplikasi</p>
     </div>
     <UButton icon="i-lucide-plus" size="sm" @click="showCreateModal = true">Tambah Toggle</UButton>
   </div>
   ```
   (Replace the existing `<div class="mb-3">` wrapper.)

6. Add the UModal at the end of the template root `<div>`:
   ```html
   <UModal v-model:open="showCreateModal" title="Tambah Feature Toggle">
     <template #body>
       <form id="create-toggle-form" class="space-y-3" @submit.prevent="handleCreate">
         <UFormField label="Label" required>
           <UInput v-model="createForm.label" placeholder="contoh: Dark Mode" />
         </UFormField>
         <UFormField label="Key" required>
           <UInput v-model="createForm.key" placeholder="dark_mode" />
         </UFormField>
         <UFormField label="Deskripsi">
           <UInput v-model="createForm.description" placeholder="Deskripsi opsional" />
         </UFormField>
         <UFormField label="Status awal">
           <USwitch v-model="createForm.enabled" />
         </UFormField>
       </form>
     </template>
     <template #footer>
       <div class="flex gap-3 justify-end w-full">
         <UButton variant="outline" @click="showCreateModal = false">Batal</UButton>
         <UButton type="submit" form="create-toggle-form" :loading="creating">Simpan</UButton>
       </div>
     </template>
   </UModal>
   ```

Note: The label field comes first in the form because typing it auto-generates the key, which the user can then review/edit below.
  </action>
  <verify>
    <automated>cd /Users/ekasetyanugraha/personal-projects/fide-cards && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>Feature toggles page shows a "Tambah Toggle" button that opens a modal with label, key (auto-generated from label), description, and enabled fields. Submitting creates the toggle via POST API and refreshes the list. Form resets on close.</done>
</task>

</tasks>

<verification>
1. `npx nuxi typecheck` passes with no errors
2. Visit /dashboard/feature-toggles as superadmin -- "Tambah Toggle" button visible
3. Click button -- modal opens with empty form
4. Type a label -- key auto-generates with snake_case
5. Submit -- new toggle appears in list
6. Try duplicate key -- error toast shown with "already exists" message
</verification>

<success_criteria>
- POST /api/feature-toggles endpoint exists, guarded by requireSuperAdmin, validates input with zod
- Feature toggles page has a "Tambah Toggle" button that opens a creation modal
- Creating a toggle refreshes the list and shows success feedback
- Duplicate keys return 409 with a user-friendly error message
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260324-i2p-add-a-button-to-create-a-new-toggle-in-t/260324-i2p-SUMMARY.md`
</output>
